$kind: http-request
name: Delete Account
method: DELETE
url: '{{base_url}}/profile'
headers:
  - key: Accept
    value: application/json
  - key: X-CSRF-TOKEN
    value: '{{csrf_token}}'
  - key: Authorization
    value: 'Bearer {{auth_token}}'
