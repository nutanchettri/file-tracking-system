$kind: http-request
name: Admin Backup Index
method: GET
url: '{{base_url}}/admin/backup'
headers:
  - key: Accept
    value: application/json
  - key: Authorization
    value: 'Bearer {{auth_token}}'
