$kind: http-request
name: Admin Create Backup
method: POST
url: '{{base_url}}/admin/backup'
headers:
  - key: Accept
    value: application/json
  - key: X-CSRF-TOKEN
    value: '{{csrf_token}}'
  - key: Authorization
    value: 'Bearer {{auth_token}}'
